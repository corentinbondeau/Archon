# Variable utilisée par l'outillage Prisma (migrate dev, migrate deploy, validate, studio).
# Chemin relatif au fichier du schéma : le fichier dev.db est créé
# dans packages/database/prisma/.
DATABASE_URL="file:./dev.db"

# Reproduis ce contenu dans un fichier .env du même dossier pour travailler en local.